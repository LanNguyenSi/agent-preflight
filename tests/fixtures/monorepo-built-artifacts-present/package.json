{
  "name": "fixture-monorepo-built-artifacts-present",
  "version": "1.2.3",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
